<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Row 1: camera-scan actions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_scan_in"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:layout_weight="1"
            android:text="@string/cash_quick_scan_in"
            android:textSize="13sp"
            app:backgroundTint="@color/biashara_success_green"
            app:icon="@drawable/ic_receipt"
            app:iconSize="16dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_scan_out"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_weight="1"
            android:text="@string/cash_quick_scan_out"
            android:textSize="13sp"
            app:backgroundTint="@color/biashara_red"
            app:icon="@drawable/ic_receipt"
            app:iconSize="16dp" />

    </LinearLayout>

    <!-- Row 2: manual-entry actions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_in"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:layout_weight="1"
            android:text="@string/cash_quick_add_in"
            android:textSize="13sp"
            app:strokeColor="@color/biashara_success_green" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_out"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_weight="1"
            android:text="@string/cash_quick_add_out"
            android:textSize="13sp"
            app:strokeColor="@color/biashara_red" />

    </LinearLayout>

    <!-- Row 3: QR card generator -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_print_qr"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="@string/cash_quick_print_qr"
        android:textSize="12sp"
        app:icon="@drawable/ic_barcode"
        app:iconSize="16dp" />

</merge>
